<?xml version="1.0" encoding="utf-8"?>
<!-- DeviceListActivity -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <include layout="@layout/titlebar_common" />

    <TextView
        android:id="@+id/tv_alarm_info_no_device_hint"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/no_device_available"
        android:textSize="18sp"
        android:visibility="gone" />

    <!--
    <Button
        android:id="@+id/btn_test_open"
        style="@style/btn_filter_condition_style"
        android:layout_width="match_parent"
        android:text="打开" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#000000" />

    <Button
        android:id="@+id/btn_test_close"
        style="@style/btn_filter_condition_style"
        android:layout_width="match_parent"
        android:text="关闭" />
    -->

    <ListView
        android:id="@+id/lv_alarm_info_device_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="#c0c0c0"
        android:dividerHeight="0.7dp">
    </ListView>

</LinearLayout>